/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Coloca aqui o domínio final quando o tiveres, para poderes usar
    // imagens externas (ex: de um CMS) além das que vivem em /public.
    remotePatterns: []
  }
};

export default nextConfig;
